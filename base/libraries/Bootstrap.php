<?php
    require(BASE . 'Common' . EXT);
    require(BASE . 'libraries' . DS . 'Loader' . EXT);
    require(BASE . 'libraries' . DS . 'Controller' . EXT);
    require(BASE . 'libraries' . DS . 'Router' . EXT);
?>